-- This is a dummy `spago.dhall` file which exists because `spago2nix generate`
-- can only run on a file named `spago.dhall`, but we want to run `spago2nix`
-- on `spago-plugin.dhall`.
../spago-plugin.dhall